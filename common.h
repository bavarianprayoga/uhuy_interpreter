#ifndef uhuy_common_h  // Include guard to prevent multiple inclusions of this header file
#define uhuy_common_h  // Define the include guard

#include <stdbool.h>   // Include the standard boolean type definitions
#include <stddef.h>    // Include the standard definitions for size_t and NULL
#include <stdint.h>    // Include the standard integer type definitions with specified widths

#endif  // End of the include guard